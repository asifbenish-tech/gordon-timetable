# לוח גורדון — כללי עבודה

מערכת שעות לבית חינוך ע"ש א.ד גורדון. המנהל: אסיף. הלוח מתפרסם למורים
מ-`master` דרך GitHub Pages: https://asifbenish-tech.github.io/gordon-timetable/

## כללי הברזל (נקבעו ע"י המנהל, 01.09.2026)

1. **שום שינוי במערכות בלי אישור מפורש.** לפני כל שינוי שנוגע בפותר או
   בנתונים: להסביר מה השינוי דורש, להריץ אותו *בצד*, ולהציג את התוצאה.
2. **להציג את היקף ההשפעה לפני הדחיפה:** כמה תאים זזו, אילו כיתות, אילו
   מורים, ומה השתנה בכל מערכת (לפני ← אחרי). סיכומים כמו "3 חוסרים" או
   "המכסות זהות" אינם תחליף - הם מסתירים כמה מערכות התהפכו.
3. **לא לפרסם לאף אחד לפני אישור.** `master` = מה שהמורים רואים. עבודה
   שלא אושרה נשארת בענף העבודה או על הדיסק בלבד.
4. **לפגוע במערכות כמה שפחות**, אלא אם זה חובה. מנגנון היציבות
   (`STAB` ב-`engine.py`) עושה את זה אוטומטית - לא לכבות אותו.
5. אחרי אישור: `python approve.py` (מעדכן את ה-baseline), ואז דחיפה
   ל-`master`. הפקודה הזו רצה *רק* באישור, לא כחלק מהצינור.

## הצינור

`python go.py` — engine → fill2 → checks → outGAPS → make_viewer → outAPI → outPDF.
`TL=<שניות>` מגבלת זמן לפותר. `STAB=0` מבטל יציבות (לבדיקות בלבד).

## מקורות אמת - מקום אחד לכל דבר

כמה באגים נבעו מעותקים כפולים שהתפצלו. אלה המקומות היחידים:

| מה | איפה |
|---|---|
| מכסות שעות | `MAXQ2` ב-`data2.py` (fill2 חסום אליו) |
| מגבלת ימי עבודה | `MAXDAYS` ב-`data2.py` |
| כינויי שמות מול האפליקציה | `APP_ALIAS` ב-`data2.py` |
| המערכת המפורסמת (ליציבות) | `baseline_J.json`, `baseline_hat.json` |
| מכסות לתצוגה | `QUOTA_FILE` ב-`make_viewer.py` |
| חריגי תוכנית לימודים | `OVR` ב-`hdata.py` (engine + checks קוראים משם) |

לא ליצור עותק חדש של אף אחד מאלה בקובץ אחר.

## פרטיות - הריפו ציבורי

- **אין תעודות זהות בגיט.** הן ב-`ids_local.json` (ב-.gitignore).
  `access_map.json` מכיל גיבובים בלבד.
- **אין תעודות זהות בלוח.** כל מה שמוטבע ב-`viewer.html` קריא לכל מי
  שיש לו את הקישור, גם בלי הזדהות. גיליון הצוות עם ת"ז נוצר מקומית
  (`staff_private.py`) ואינו נכנס לגיט.
- `app_data/` (נתוני האפליקציה: היעדרויות, בקשות חופשה, ת"ז) - ב-.gitignore.

## דברים שנלמדו בדרך

- `index.html` מפנה ל-`viewer.html?v=<Date.now()>` - החותמת מחושבת בדפדפן.
  חותמת שמוטבעת בזמן בנייה נתקעת במטמון ומגישה מערכת ישנה.
- תא חוסר מציג "חסר מורה" + סיבה כשהמחנך/ת תפוס/ה במקום אחר. להציג את
  המחנך/ת ככיסוי כשהיא מלמדת כיתה אחרת = לשבץ אותה בשתי כיתות בו-זמנית.
- קובצי ה-PDF של הלשוניות דורשים עקיפת מסך ההזדהות ב-`outPDF.py`,
  אחרת כולם יוצאים תמונה של מסך הכניסה.
- הבנייה דטרמיניסטית: אותו קלט = אותו `viewer.html`. אם דיף מראה שינוי
  בלי סיבה, משהו איטרר על set בלי מיון.
- כשהמנהל מדווח שהוא רואה משהו שונה ממה שאני מדווח - קודם לבדוק את
  חותמת הגרסה בכותרת הלוח. כמעט תמיד זה מטמון.

## הזדהות

PBKDF2-SHA256, מלח `gordon-lohot-2026`, 100 אלף איטרציות. תפקידים ב-`gen_access.py`:
הנהלה (רואה הכל): אסיף, דאפי, חסן, מיכל. רכזי בתים: לייה (א), שרית (ב), אלי (ג).
